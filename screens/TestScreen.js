import React from "react";
import { View, Text, StyleSheet } from "react-native";

const TestScreen = (props) => {
  return (
    <View>
      <Text>New TestScreen</Text>
    </View>
  );
};

export default TestScreen;

const styles = StyleSheet.create({});
