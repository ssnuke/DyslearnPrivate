import React from "react";
import { View, Text, StyleSheet } from "react-native";

const Reader = (props) => {
  return (
    <View>
      <Text>New Reader</Text>
    </View>
  );
};

export default Reader;

const styles = StyleSheet.create({});
