import React from "react";
import {View,Text} from "react-native";

const ImageReader = (props) => {
    return (
        <View>
            <Text>Hello from image reader</Text>
        </View>
    );
};

export default ImageReader;
